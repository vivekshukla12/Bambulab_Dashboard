# Synthetic Test Fixtures

All repository fixtures are synthetic by default.

Fixtures should:
- represent realistic data shapes;
- exercise edge/failure cases;
- use fake identifiers and credentials;
- be deterministic where practical;
- clearly state that they are synthetic;
- never be generated by copying live printer/cloud/customer output into the repository.
