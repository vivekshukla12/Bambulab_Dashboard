# Tests

Test implementation begins with the first authorized implementation milestone.

Baseline policy:
- deterministic where practical;
- meaningful success/failure/edge/regression coverage;
- no secrets or real customer/production data;
- real-printer tests only when explicitly authorized and isolated from default staging/test flows.
